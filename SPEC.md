# Cube Trainer — 제품 명세 (SPEC)

상태: 큐브 시뮬레이터 + 직관적 F2L 페어링 훈련 · 현재 제품 동작의 기준 문서

## 문서의 역할

이 문서는 PRD의 제품 목표·범위뿐 아니라 제품 지침, 제약 조건, 기능 요구사항, 테스트 수용 기준(acceptance criteria)을 함께 관리한다. 기존 PRD는 이 문서로 통합했다.

- 제품이 무엇을 해야 하는지와 변경 후 만족해야 할 조건은 이 문서에 기록한다.
- 개발·협업·Git·검증 절차는 [AGENTS.md](AGENTS.md), 실행 방법은 [README](README.md)에 기록한다.
- 구현 방식과 선택 이유는 [기술 구조](docs/ARCHITECTURE.md), 앞으로 할 일은 [ROADMAP](docs/ROADMAP.md)에 기록한다.
- 제품 동작을 변경할 때 관련 요구사항과 수용 기준을 함께 갱신한다. 다른 문서에 제품 규칙의 별도 사본을 만들지 않고 이 문서를 참조한다.

## 제품 목표

실제 회전 규칙에 맞는 3×3 큐브를 데스크톱·모바일에서 직접 돌리며 연습한다. 자유 연습에 더해, 준비된 W 십자가에서 직관적 F2L의 Y층 페어링을 익힌다. 슬롯 정렬·삽입은 후속 단계다. 초보 1층 → 2층 → 3층 풀이와 CFOP의 C(십자가 만들기)는 건너뛴다.

## 표시 규칙 — 확정

- 항상 동일한 큐브를 두 시점으로 함께 표시한다. 하나는 기준 그림, 하나는 뒤집힌 그림이다.
- 기본 방향은 U 노랑, D 흰색, F 빨강, R 초록, B 주황, L 파랑이다.
- 큰 그림은 F/R/U를 보여 준다. 앞면이 옆면보다 넓게 보여야 한다.
- 작은 그림은 B/L/D를 보여 준다. 화면 위쪽에 보이는 흰색 면은 원본 기준 D이며, 주황 B가 앞, 파랑 L이 왼쪽에 보인다.
- 작은 그림은 큰 그림보다 작게 표시한다. 큐브 블록과 센터 위에는 면 기호 F/U/R/D/L/B를 표시하지 않는다. C/E 조각 식별자도 표시하지 않는다. 대상 조각에는 색 문자 W/Y/R/O/B/G만 표시하며, 방향은 그림 밖 범례와 조작 버튼으로 안내한다.
- 모바일에서는 조작 패널로 스크롤해도 두 그림이 붙어 다니도록 표시한다.
- 두 그림의 상태·애니메이션은 같은 논리 상태에서 계산한다. 사진 파일을 런타임에서 사용하지 않는다.
- 브라우저 탭에는 투명 배경에 노랑 위·빨강 앞·초록 오른쪽의 큐브 파비콘을 표시한다. 로컬과 GitHub Pages의 하위 경로 모두에서 정상 로드되어야 한다.

## 조작 규칙 — 확정

| 입력                  | 동작                                               |
| --------------------- | -------------------------------------------------- |
| f / r / u / d / l / b | 그 면의 밖에서 바라본 시계 방향 90°                |
| Shift + 문자 키       | 그 면의 반시계 방향 90°                            |
| →                     | 윗면에서 내려다본 전체 큐브의 시계 방향 90° 회전   |
| ←                     | 윗면에서 내려다본 전체 큐브의 반시계 방향 90° 회전 |
| 모바일 십자 버튼      | 중앙 F, 왼쪽 L, 위 U, 오른쪽 R, 아래 D             |
| 작은 B 버튼           | U와 R 사이에 배치                                  |
| 모바일 돌림 아이콘    | 전체 큐브를 좌·우로 90° 회전                       |

모바일의 역회전은 시계/반시계 방향 선택 버튼으로 제공한다. 이 선택은 화면 버튼에만 적용하며, 키보드는 항상 Shift 여부로 방향을 결정한다.

전체 수평 회전은 실제 큐브를 손으로 돌리는 것처럼 앞·옆·뒤의 기준을 바꾼다. 오른쪽(→) 회전 후에는 기존 오른쪽 면이 F가 되고, 이후 F 입력은 새 앞면을 돌린다. 위·아래 센터는 계속 노랑·흰색이다. 면 회전으로 가장자리·꼭짓점 스티커가 다른 면으로 이동하는 것은 정상이다.

전체 회전 방향은 작은 뒤집힌 그림이 아니라 **메인 큐브의 노란 윗면(U)에서 내려다보는 기준**이다. 기본 상태에서 다음 결과가 나와야 한다.

| 입력                    | 새 F | 새 R | 새 B | 새 L | U / D            |
| ----------------------- | ---- | ---- | ---- | ---- | ---------------- |
| → 또는 오른쪽 돌림 버튼 | 초록 | 주황 | 파랑 | 빨강 | 노랑 / 흰색 유지 |
| ← 또는 왼쪽 돌림 버튼   | 파랑 | 빨강 | 초록 | 주황 | 노랑 / 흰색 유지 |

키보드와 화면 돌림 버튼은 같은 방향으로 작동한다. 이력의 화살표도 입력 방향을 나타내며, 한 수 되돌리기는 반대 방향으로 복원한다. 화면의 면 회전 방향 선택과 Shift는 전체 수평 회전 방향을 바꾸지 않는다.

중간층 회전, 큐브 전체를 세로로 뒤집는 조작, 임의 시점 드래그는 이번 범위에 없다. 두 번째 그림의 뒤집힘은 관찰 시점이며 큐브 상태를 따로 변경하지 않는다.

## 시작 상태 — 확정

| 프리셋      | 보장할 상태                                   | 이번 생성 방식                                                    |
| ----------- | --------------------------------------------- | ----------------------------------------------------------------- |
| 맞춘 큐브   | 여섯 면 완성, 기본 방향                       | 완료 상태 생성                                                    |
| 랜덤 섞기   | 실제로 도달 가능한 섞인 큐브                  | 같은 면을 연속 선택하지 않는 25수; 대회용 무작위 상태 분포는 아님 |
| 흰색 십자가 | D의 흰색 엣지 네 개와 각 옆면 센터 정렬       | 십자가를 보존하는 회전으로 나머지 조각을 섞음                     |
| 1·2층 완성  | 흰색 아래 기준 하단 두 층 완성, 상단은 미완성 | U 회전과 아래 두 층을 보존하는 수순 적용                          |

프리셋을 누르면 이전 수순과 대기 입력을 비우고 기본 방향으로 새로 시작한다. 버튼 아래에 이 동작을 안내한다. 시작 상태를 만든 수순은 도움말에서 확인할 수 있다. 랜덤 프리셋에는 불가능한 스티커 배열을 사용하지 않는다.

## 보조 기능 — 이번 구현

- 모든 면 회전과 전체 수평 회전을 한 입력 큐로 처리한다. 큐는 최대 20수이며 초과 입력은 안내하고 거절한다.
- 보통/느린 회전, 모션 감소 환경에서는 즉시 결과 표시.
- 실행된 수순 표시와 한 수 되돌리기. 전체 회전도 이력에 기록하되 면 회전 횟수에는 포함하지 않는다.
- 되돌리기는 회전 대기 중 비활성화하며, 프리셋을 바꾸면 이전 이력은 지운다.
- **수순 리셋**은 마지막으로 완료된 큐브 상태·전체 방향·훈련 진행을 유지하고 이력·회전 횟수·대기 입력만 비운다. 진행 중 애니메이션은 취소하고 늦게 도착한 프레임은 무시한다. 리셋 이전 이력은 되돌릴 수 없다.
- **현재 시작 상태로 되돌리기 / 같은 배치 다시**는 큐브까지 처음 배치·방향으로 복원하며 이력과 대기 입력을 비운다. 랜덤 시작 상태도 새로 생성하지 않고 같은 배치를 복원한다.
- 현재 상태가 전체 완성, 1·2층 완성, 흰색 십자가 완성인지 표시한다.
- 키보드 반복 입력, 조합 입력, Ctrl/Alt/Meta 단축키, 입력 필드 타이핑은 회전으로 처리하지 않는다.

## F2L 페어링 훈련 — 확정

### 범위와 출발 조건

- 초보 층별 풀이와 C 단계, 조각을 Y층으로 꺼내 분리하는 과정은 생략한다. 모든 시작 상태는 **W 십자가 네 엣지와 O/B/R/G 센터가 정렬**되어 있다.
- 설명·강조 대상은 항상 **R·G·W 코너와 R·G 엣지**다. 다른 색 조각의 예제로 바꾸지 않는다.
- 코너는 항상 Y층 앞·오른쪽(UFR), 자기 R/G 슬롯 바로 위다. 엣지는 항상 Y층 뒤쪽(UB), **O 센터 위**다. 유형 전환이나 5번의 윗색 변경으로 이 위치를 바꾸지 않는다.
- 기본 방향은 Y 위, W 아래, R 앞, G 오른쪽, O 뒤, B 왼쪽이다. 시작 상태는 합법적 회전으로 생성한다.
- 이번 목표는 **두 조각을 Y층에서 같은 색끼리 연결한 페어**다. 슬롯 정렬·삽입, 좌우 삽입 공식, 삽입 완료 판정을 제공하지 않는다.

### 다섯 학습 유형과 수순

번호는 고정 시점에서 W 방향과 두 조각의 윗색으로 결정한다. 전체 회전·대칭을 이유로 번호를 통합하거나, 엣지를 UL로 옮겨 다른 사례로 대체하지 않는다.

| 유형 | 코너 W 방향      | 코너/엣지 윗색 | Y층 페어링 수순 | 수  |
| ---- | ---------------- | -------------- | --------------- | --- |
| 1    | 오른쪽 G 센터 쪽 | G / R, 다름    | R U′ R′         | 3   |
| 2    | 오른쪽 G 센터 쪽 | G / G, 같음    | U F′ U2 F       | 4   |
| 3    | 앞 R 센터 쪽     | R / G, 다름    | U′ R′ F R F′    | 5   |
| 4    | 앞 R 센터 쪽     | R / R, 같음    | U′ R U R′       | 4   |
| 5    | 위 Y 센터 쪽     | W / R          | U R U2 R′       | 4   |
| 5    | 위 Y 센터 쪽     | W / G          | U2 F′ U′ F      | 4   |

5번은 엣지의 두 방향을 한 항목에서 제공한다. ‘엣지 윗색 바꾸기’로 R/G만 바꾸며 위치는 UB로 유지한다. 1~4번에는 배치 변경 버튼이 없다.

### 최소 수순의 기준과 완료 판정

- 최소 수순은 **Y층 페어링 + W 십자가 복원 + 다른 세 슬롯의 코너·엣지 보존**을 만족하는 바깥 면 회전 기준이다. 90°/180° 각각 한 수(HTM)로 세며, 전체 큐브 회전·중간층 회전은 해법에 사용하지 않는다. 더 짧은 모든 바깥 면 회전 수순을 검사해 최소성을 검증한다.
- 다른 세 코너가 이미 맞춰져 있어도 어느 하나 흐트러진 채 끝나면 안 된다. 해당 슬롯의 엣지도 함께 보존한다. 모든 연습은 다른 세 슬롯까지 완성된 상태에서 시작한다. 회전 중 일시 이동은 가능하지만 전체 페어링 수순 완료 후에는 각 스티커의 위치와 방향이 시작 상태와 정확히 같아야 한다.
- 1번은 R 한 번에 두 조각이 붙지만 십자가가 일시 변형된다. U′로 페어를 비키고 R′로 십자가를 복원하면 완료다. R U R′로 슬롯에 넣는 해법은 이번 목표가 아니다.
- 2·4번은 U/U′ 준비 후 코너를 숨기고 엣지를 옮겨 만난다. 3번은 U′ 준비 후 R′ F R F′로 코너를 움직이며 면을 복원한다. 5번은 U/U2 준비 후 엣지를 내리고 코너를 옮긴 뒤 다시 올린다.
- 두 조각이 Y층에서 인접하고 R/G 스티커 방향이 각각 일치해야 페어다. 단순 인접, 슬롯 삽입, 수순 실행 횟수로 완료를 판정하지 않는다. 페어 연결·십자가 복원·다른 세 슬롯 보존을 각각 판정한다. 페어와 십자가가 맞아도 다른 코너나 엣지가 흐트러져 있으면 완료로 처리하지 않고 복원을 안내한다.
- 안내는 페어링 단일 단계다. 완료 시 시연 버튼을 비활성화하고 Y층에서 멈춘다. 다른 정답 수순으로 만들어도 실제 상태로 완료를 판정한다.
- 키보드·버튼·시연은 같은 입력 큐를 사용한다. U2는 수동 U 두 번으로 따라갈 수 있다. ‘혼자 연습’은 해설·수순을 숨긴다. 수순 리셋은 큐브와 진행을 유지하며 기록·대기 입력만 비운다.

## 제품 지침과 제약 조건

- 데스크톱과 모바일에서 같은 핵심 연습을 제공한다. 한국어 UI를 기본으로 한다.
- 최소 화면 너비는 320 CSS px다. 두 그림의 동시 표시와 조작 접근성을 우선하고 페이지 전체의 가로 스크롤을 만들지 않는다.
- 색상만으로 방향을 전달하지 않는다. 그림 밖 방향 범례, 버튼의 접근 가능한 이름, 키보드 초점 표시를 유지한다. 작은 B를 포함한 면 회전 버튼의 터치 영역은 최소 44×44 CSS px다.
- 상태 계산이 기준이며 애니메이션은 그 변화를 표현한다. 두 그림의 상태나 입력 경로가 서로 어긋나면 안 된다.
- 큐브를 임의의 색상 배열로 만들지 않는다. 스티커·조각의 보존, 실제 회전으로 도달 가능함을 검증해야 한다.
- 현재 제품은 외부 계정·서버·환경 변수 없이 브라우저에서 실행한다. 참고 사진을 필수 자산으로 사용하지 않는다.
- 현재 지원하는 것은 여섯 바깥 면과 전체 수평 회전이다. 중간층·전체 세로 뒤집기·임의 시점 드래그는 제외한다.
- 임의 상태의 자동 풀이, 전체 풀이 강좌, 타이머·통계, 로컬 저장, 계정·동기화, PWA·오프라인, 다른 크기 큐브는 후속 범위다. 현재 수순 안내는 위 F2L 연습 배치에 한정한다.
- 모바일 브라우저 에뮬레이션의 통과를 실제 iOS/Android 터치감·성능 검증으로 간주하지 않는다.

## 배포와 운영 제약

- GitHub Pages에서 정적 앱으로 제공한다. 기본 서비스 주소는 `https://daekyuny.github.io/cube_trainer/`다.
- 앱과 정적 자산은 `/cube_trainer/` 하위 경로에서 정상 로드되어야 한다. 새로고침으로 큐브나 컨트롤이 사라지면 안 된다.
- `main`의 검증이 모두 통과한 빌드만 자동 배포한다. PR과 다른 브랜치의 수동 실행은 검증만 한다.
- 배포는 서버·DB·클라이언트 비밀 키를 추가하지 않는다. 동일 빌드를 테스트하고 배포하며, 검사 실패 시 기존 서비스에 새 결과를 배포하지 않는다.
- 사용자 인증·서비스 데이터 수집 등 제품 범위는 정적 배포만으로 확대하지 않는다.

## 테스트 수용 기준 (Acceptance criteria)

아래 ID는 제품 규칙과 검증 항목을 연결한다. 자동화된 항목은 관련 테스트로, 시각·실기기 항목은 해당 환경에서 확인한다. 목록 자체가 모든 항목의 검증 완료를 의미하지는 않는다.

| ID         | 조건과 기대 결과                                                                                                                                             | 검증 방법                   |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------- |
| VIEW-01    | 완료 상태에서 큰 그림은 F 빨강/R 초록/U 노랑, 작은 그림은 B 주황/L 파랑/D 흰색이다. 앞면이 옆면보다 넓고 작은 그림이 원본보다 작다.                          | 브라우저 + 시각 확인        |
| VIEW-02    | 320/390/768/1440px에서 가로 넘침 없이 두 그림이 함께 표시되고 조작 중에도 보인다.                                                                            | 브라우저 + 시각 확인        |
| ROT-01     | 각 면의 네 번 회전, 회전+역회전, 수순+역수순이 원상복구되고 두 번의 90° 회전은 180° 회전과 같다.                                                             | 엔진 단위 테스트            |
| ROT-02     | 알려진 단일 회전의 엣지 이동이 해당 면을 밖에서 바라본 시계 방향과 일치하고 스티커 위치·조각이 보존된다.                                                     | 엔진 단위 테스트            |
| YAW-01     | 기본 상태에서 →와 오른쪽 버튼은 시계 방향으로 돌아 F 초록/R 주황/B 파랑/L 빨강이 된다. U/D는 노랑/흰색을 유지하고 이력은 →다.                                | 엔진 + 브라우저 회귀 테스트 |
| YAW-02     | 기본 상태에서 ←와 왼쪽 버튼은 반시계 방향으로 돌아 F 파랑/R 빨강/B 초록/L 주황이 된다. U/D는 유지되고 이력은 ←다.                                            | 브라우저 회귀 테스트        |
| YAW-03     | 양쪽 방향 모두 네 번 돌리면 두 그림이 원상복구된다. 한 수 되돌리기도 원래 방향을 복원한다. 전체 회전은 면 회전 횟수에 포함하지 않는다.                       | 엔진·세션 + 브라우저        |
| YAW-04     | 전체 수평 회전 후 F/R/L/B 입력은 새 기준 면을 돌린다. 키보드와 화면 버튼의 결과가 같아야 한다.                                                               | 엔진 + 브라우저             |
| INPUT-01   | 문자 키와 Shift 조합은 정·역회전하고 화면 방향 선택은 화면의 면 버튼에만 적용된다. 조합 입력·텍스트 입력·브라우저 단축키·키 반복을 회전으로 처리하지 않는다. | 브라우저 + 키보드 확인      |
| INPUT-02   | 모바일 십자는 중앙 F, 왼쪽 L, 위 U, 오른쪽 R, 아래 D이며 B는 U/R 사이다. 최소 터치 영역·접근 가능한 이름·초점 표시를 유지한다.                               | 브라우저 + 터치·시각 확인   |
| PRESET-01  | 맞춘 큐브는 여섯 면 완성이다. 랜덤은 합법적인 25수로 생성한 미완성 상태이며 수순으로 재현·역복원이 가능하다.                                                 | 엔진 단위 테스트            |
| PRESET-02  | 흰색 십자가는 D의 엣지 네 개와 옆면 센터가 정렬되고 나머지 층은 연습할 상태다.                                                                               | 엔진 단위 테스트            |
| PRESET-03  | 1·2층 프리셋은 하단 두 층이 완성되고 상단은 미완성이다.                                                                                                      | 엔진 단위 테스트            |
| SESSION-01 | 두 그림은 동일한 회전을 함께 애니메이션한다. 연속 입력을 순서대로 한 번씩 적용하고 최대 20수 초과 입력은 안내 후 거절한다.                                   | 세션 + 브라우저·시각 확인   |
| SESSION-02 | 회전 중 프리셋을 바꾸면 기본 방향의 새 상태, 빈 이력·입력 큐로 전환된다. 이전 프레임은 새 상태를 덮어쓰지 않는다.                                            | 세션 + 브라우저             |
| ACCESS-01  | 느린 회전이 동작하고 모션 감소 설정에서는 즉시 결과를 표시한다. 속도 설정 후에도 키보드 면 회전이 작동한다.                                                  | 브라우저 + 설정별 확인      |
| DEVICE-01  | 실제 iOS/Android에서 터치·스크롤·가로 화면의 조작을 확인하고, 성능 문제와 확인 기기를 기록한다.                                                              | 실기기 검증; 후속 확인 필요 |

F2L 및 리셋 수용 기준:

| ID         | 조건과 기대 결과                                                                                                                                                                                                                             | 검증 방법                   |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------- |
| F2L-01     | 다섯 유형의 모든 시작 상태에서 기본 센터 방향, W 십자가·다른 세 슬롯 정렬, R/G/W 코너 UFR 및 R/G 엣지 UB를 보장한다. 합법적 생성과 역복원이 가능하다.                                                                                        | 엔진 + 브라우저             |
| F2L-02     | 1·2번은 W 오른쪽, 3·4번은 W 앞, 5번은 W 위다. 1·3번 윗색은 다르고 2·4번은 같다. 5번의 엣지 윗색 R/G 두 방향은 같은 번호와 같은 위치를 유지한다.                                                                                              | 엔진 + 브라우저             |
| F2L-03     | 모든 안내 수순은 Y층의 실제 페어, 복원된 W 십자가, 보존된 다른 세 코너·엣지에서 끝난다. 슬롯 정렬·삽입 단계가 없고 완료 뒤 시연을 진행하지 않는다. 임의 정답과 전체 수평 회전에도 상태 판정이 유지된다.                                      | 엔진 + 브라우저             |
| F2L-04     | 수동 입력·시연·U2·되돌리기와 안내 커서가 일치한다. 잘못된 경로에서 시연을 차단하고 되돌리면 복구된다. 유형 변경·재시작·모드 전환은 대기 입력과 오래된 프레임을 취소한다.                                                                     | 엔진 + 브라우저             |
| F2L-05     | 320/390/768/1440px 및 844×390에서 두 큐브를 보며 선택·조작·시연·다시하기가 가능하고 가로 넘침이 없다. 터치 버튼 높이는 최소 44px다.                                                                                                          | 브라우저 + 시각             |
| F2L-06     | 90°/180°를 한 수로 세고 여섯 바깥 면의 더 짧은 모든 수순을 탐색했을 때 Y층 페어·W 십자가·다른 세 코너와 엣지의 보존을 동시에 만족하는 해법이 없어야 한다.                                                                                    | 엔진 최단 수순 회귀 테스트  |
| F2L-07     | 1번 R 후에는 페어 연결·십자가 복원 중을 표시하고 U′ R′ 후 완료한다. R U R′로 슬롯에 넣은 상태를 Y층 페어링 완료로 판정하지 않는다.                                                                                                           | 엔진 + 브라우저             |
| F2L-08     | 두 큐브 모두 블록·센터 위에 면 기호 F/U/R/D/L/B가 없다. C/E 식별자도 없고 강조 시 색 문자만 표시하며 강조 해제·자유 연습에서도 면 기호가 나타나지 않는다.                                                                                    | 브라우저 + 시각             |
| F2L-09     | 모든 배치에서 다른 세 코너·세 엣지의 모든 스티커 위치·방향을 시작과 완료 시 비교한다. 과거 수순 R′ U2 R 등은 페어·십자가가 맞아도 다른 슬롯이 깨지므로 완료 판정을 거부한다. 세 코너 각각에 대한 오류 재현과 네 수평 방향의 판정을 검증한다. | 엔진 + 브라우저 회귀 테스트 |
| SESSION-03 | 수순 리셋은 큐브·방향·훈련 진행을 유지하고 이력·대기 입력만 비운다. 진행 중 오래된 프레임은 큐브를 바꾸지 않는다.                                                                                                                            | 세션 + 브라우저             |
| SESSION-04 | 재시작은 랜덤을 포함한 정확히 같은 시작 배치·방향으로 복원한다. 이력·입력 큐·훈련 진행도 초기화한다.                                                                                                                                         | 세션 + 브라우저             |

배포 수용 기준:

| ID        | 조건과 기대 결과                                                                                                                | 검증 방법                                   |
| --------- | ------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- |
| DEPLOY-01 | Pages 빌드가 `/cube_trainer/`에서 HTML·JS·CSS 오류 없이 로드되고 새로고침 후 두 큐브와 회전 입력이 동작한다.                    | 빌드 결과의 브라우저 테스트 + 공개 URL 확인 |
| DEPLOY-02 | 같은 커밋의 코드 검사·단위 테스트·일반/Pages 브라우저 테스트를 통과한 결과만 `main`에서 배포한다. PR은 배포 권한을 받지 않는다. | 워크플로 검토 + Actions 실행 결과           |
| DEPLOY-03 | Pages로 배포해도 일반 개발 서버는 루트 경로에서 동작한다. 별도 클라이언트 비밀정보가 필요하지 않다.                             | 일반/Pages 빌드 및 브라우저 검증            |

자동화 위치: `tests/cube.test.ts`(엔진), `tests/f2l.test.ts`(훈련 배치·페어링·삽입), `tests/session.test.ts`(세션), `tests/browser/`(화면·입력·훈련). 실행 명령과 변경별 필수 검증 절차는 [AGENTS.md](AGENTS.md)를 따른다.

## 다음 논의

다섯 대표 유형의 보존 조건을 만족하는 Y층 페어링 수순을 연습하고, 슬롯 삽입은 다음 단계로 추가한다. 다른 색 페어, 네 페어 연속 연습, 임의 배치에서의 힌트, 로컬 저장은 이후 논의한다. 초보 층별 풀이와 C 강좌는 현재 목표가 아니며 전체 풀이 강좌·타이머·통계·계정·PWA·다른 크기 큐브도 후속 범위다.
