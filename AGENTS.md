# Cube Trainer 작업 지침

이 파일은 저장소 전체의 공통 지침이다. 어느 PC에서 작업하든 대화 기록이나 로컬 메모리 대신 이 파일과 저장소 문서로 프로젝트 맥락을 복원한다. 사용자의 새 요구로 제품 규칙이 바뀌면 구현과 관련 문서를 함께 갱신한다.

## 작업 시작

1. `git status --short --branch`, `git remote -v`, 최근 커밋을 확인한다. 기존 변경은 사용자의 작업일 수 있으므로 보존한다.
2. [README](README.md)로 실행 방법, [PRD](docs/PRD.md)로 제품 규칙, [기술 구조](docs/ARCHITECTURE.md)로 구현 경계를 확인한다. 다음 과제는 [ROADMAP](docs/ROADMAP.md)을 참고한다.
3. 여러 PC에서 변경될 수 있으므로 가능하면 `git fetch origin`으로 원격 상태를 확인한다. 작업 트리가 깨끗하고 현재 브랜치가 원격보다 뒤에만 있을 때 `git pull --ff-only`로 동기화한다. 서로 다른 변경이 있으면 내용을 확인해 보존하며 통합한다.
4. 요청 범위를 파악하고 관련 코드·테스트를 읽은 뒤 수정한다. 이전 PC의 경로, 실행 중인 서버, 설치된 도구를 가정하지 않는다.

## 협업 원칙

- 사용자와의 대화 및 프로젝트 문서는 한국어를 기본으로 한다. 코드 식별자는 영어로 작성한다.
- 구현 요청은 실제 수정과 필요한 검증까지 진행한다. 진행 상황과 발견한 문제는 간결하게 공유한다.
- 되돌릴 수 있는 세부 구현은 합리적으로 결정하고, 제품 동작을 크게 바꾸는 불명확한 요구만 질문한다. 이미 승인된 작업을 반복해서 확인받지 않는다.
- 요청과 무관한 리팩터링, 대규모 의존성 변경, 새 서비스 연결을 섞지 않는다.
- 최종 보고에는 변경 내용, 실행한 검증, 남은 제약을 적는다. 실행하지 못한 검증을 통과했다고 표현하지 않는다.

## 개발 환경과 이식성

- Node.js **24.x**와 npm을 사용한다. 버전 기준은 `.nvmrc`와 `package.json`이다. nvm이 있으면 `nvm use`, 없으면 해당 PC의 설치 방법으로 Node 버전을 맞춘다.
- 처음 clone했거나 의존성 잠금 파일이 바뀌면 저장소 루트에서 `npm ci`를 실행한다. 의존성을 의도적으로 추가·변경할 때는 `package.json`과 `package-lock.json`을 함께 갱신한다.
- 명령은 저장소 루트 기준으로 작성한다. 사용자 홈 절대 경로, 다른 프로젝트의 `node_modules`, 특정 PC의 브라우저 캐시·임시 라이브러리에 의존하지 않는다.
- 개발 서버는 `npm run dev`. 기본 포트는 5173이지만 사용 중이면 달라지므로 실제 출력된 주소를 확인한다. 모바일 접속은 `npm run dev -- --host 0.0.0.0`과 같은 네트워크의 주소를 사용한다.
- 브라우저 테스트 준비는 `npx playwright install chromium`. Linux에서 시스템 라이브러리가 부족하면 `npx playwright install --with-deps chromium`을 사용한다. OS 권한·샌드박스 제한은 해당 환경의 승인 절차를 따른다.
- 환경에 필요한 임시 조치는 제품 코드에 하드코딩하지 않는다. 다른 PC에서도 필요한 공통 설치 사항만 README에 남긴다.
- 현재 앱은 외부 서비스·환경 변수 없이 실행된다. 인증 정보, `.env`, 로컬 Codex 설정, 빌드 결과와 테스트 산출물은 커밋하지 않는다.

## 유지할 제품 규칙

아래는 사용자가 정한 현재 기준이다. 사용자의 변경 요청이 없는 한 일반적인 큐브 색상 배치나 다른 조작 방식으로 교체하지 않는다.

- 3×3 큐브 하나를 **두 시점으로 동시에** 보여 준다. 두 그림은 같은 논리 상태와 애니메이션 진행률을 공유한다.
- 기본 방향: **U 노랑 / D 흰색 / F 빨강 / R 초록 / B 주황 / L 파랑**.
- 큰 그림은 **F/R/U**를 보여 주며, 앞면을 옆면보다 넓게 투영한다.
- 작은 그림은 뒤집힌 **B/L/D**를 보여 준다. 주황 B가 앞, 파랑 L이 왼쪽, 흰색 D가 화면 위쪽에 보인다. 원본보다 작게 표시한다.
- 작은 그림은 관찰 시점만 다르다. 별도 큐브 상태나 독립된 회전을 만들지 않는다. 모바일 조작 중에도 두 큐브가 보이게 한다.
- `f r u d l b`는 해당 면의 **밖에서 바라본 시계 방향 90°**, `Shift`와 조합하면 반시계 방향이다.
- `←` / `→`와 화면 돌림 버튼은 전체 큐브의 수평 90° 회전이다. 기존 옆면이 새 앞면이 되며 이후 F/R/L/B 입력도 새 기준을 따른다. U/D 센터는 노랑/흰색으로 유지한다.
- 위·아래 색상 고정은 센터와 기준 방향을 뜻한다. 면 회전으로 다른 스티커가 움직이는 것은 정상이다.
- 모바일 십자 컨트롤러는 중앙 F, 왼쪽 L, 위 U, 오른쪽 R, 아래 D. 작은 B는 U와 R 사이에 두며 터치 영역은 최소 44px로 유지한다.
- 화면의 시계/반시계 선택은 화면 버튼에 적용하고, 키보드는 Shift 여부로 결정한다. 입력 필드 타이핑, 조합 입력, 브라우저 단축키를 가로채지 않는다.
- 프리셋: 맞춘 큐브, 합법적인 랜덤 섞기, 옆면 센터까지 정렬된 흰색 십자가, 흰색 아래 기준 1·2층 완성. 모두 실제 회전으로 도달 가능한 상태여야 한다.
- 프리셋 변경은 기본 방향으로 새로 시작하며 수순과 대기 입력을 비운다. 이전 애니메이션이 새 상태를 덮어쓰면 안 된다.
- 중간층 회전, 전체 세로 뒤집기, 임의 시점 드래그는 현재 범위에 없다.
- 참고 사진은 실행에 필요하지 않다. 삭제된 이미지나 원래 대화 첨부파일을 필수 자산으로 요구하지 않는다.

## 코드 구성과 구현 원칙

| 위치                                  | 책임                                        |
| ------------------------------------- | ------------------------------------------- |
| `src/domain/cube/`                    | 순수 상태 엔진, 회전, 프리셋, 완성 판정     |
| `src/features/simulator/CubeView.tsx` | 3D 좌표의 SVG 투영과 두 시점 표현           |
| `src/features/simulator/session.ts`   | 입력 큐, 애니메이션 완료, 이력, 프리셋 전환 |
| `src/app/`                            | 화면 배치, 키보드·터치 연결, 반응형 스타일  |
| `tests/`                              | 엔진·세션 단위 테스트와 브라우저 테스트     |

- TypeScript의 엄격한 타입 검사를 유지한다. 기존 React·CSS·SVG 구성을 우선 활용하고, 필요가 명확할 때만 의존성을 추가한다.
- 도메인 로직은 React, DOM, 타이머, SVG에 의존하지 않게 한다. 논리 상태를 먼저 정확하게 계산하고 화면이 이를 표현하게 한다.
- 현재 상태 모델은 54개 스티커의 ID·정수 위치·법선·색상이다. 좌표는 +X 오른쪽, +Y 위, +Z 앞이다. 엔진을 바꿀 때 이 기준과 조각 보존을 검증한다.
- 키보드와 화면 버튼은 같은 명령 처리 경로를 사용한다. 입력 순서, 최대 20수 대기 큐, 완료 시 한 번만 상태 반영, 오래된 프레임 무시를 유지한다.
- 색상만으로 조작을 설명하지 않는다. 면 문자, 접근 가능한 버튼 이름, 키보드 초점, 모션 감소 설정을 유지한다.
- 텍스트·설정만 바꾸는 작업에 구현을 그대로 복제한 테스트를 추가하지 않는다. 버그 수정은 원인을 재현하는 회귀 테스트를 필요한 수준으로 추가한다.

## 변경별 검증

| 변경                       | 수행할 검증                                                                    |
| -------------------------- | ------------------------------------------------------------------------------ |
| 문서만 수정                | 변경 문서의 Prettier 검사, 링크·명령·내용 확인, `git diff --check`             |
| 코드·설정·의존성 수정      | `npm run check`, `npm test`, `npm run build`                                   |
| 큐브 엔진·프리셋·세션 수정 | 위 검사와 함께 회전 방향, 역수순 복원, 프리셋 제약, 초기화·입력 순서 회귀 검증 |
| 화면·입력·애니메이션 수정  | 위 검사와 `npm run test:e2e`, 관련 화면 시각 확인                              |

- 엔진은 4회 회전, 회전+역회전, 알려진 단일 회전의 조각 이동, 전체 회전 후 기준 면, 프리셋의 가역성을 확인한다.
- 화면은 320/390/768/1440px에서 두 큐브 표시, 가로 넘침, 컨트롤 접근성을 확인한다. 터치·키보드, 회전 도중 프리셋 전환도 살핀다.
- 실제 iOS/Android의 조작감·성능 확인과 브라우저 에뮬레이션을 구분한다.
- 포맷 수정은 변경 파일에 한정하는 것을 우선한다. 검증이 통과한 후에는 추가 변경이나 해결되지 않은 문제가 없으면 불필요하게 반복하지 않는다.
- 검증이 환경 문제로 막히면 원인과 미검증 범위를 명시한다. 테스트를 끄거나 무관한 기존 코드를 바꿔 통과시키지 않는다.

## Git과 PC 간 작업 인계

- 커밋·푸시는 현재 요청이나 이어지는 작업에서 승인된 범위대로 수행한다. 이 문서 자체를 모든 향후 작업에 대한 배포·푸시 승인으로 해석하지 않는다.
- 커밋 전 `git diff`와 staged diff를 확인하고 작업에 해당하는 파일만 포함한다. 참고 사진·임시 스크린샷·개인 설정은 제외한다.
- 커밋 제목은 실제 변경을 설명한다. 기존 형식처럼 `feat:`, `fix:`, `docs:`, `test:`, `chore:`를 사용할 수 있다.
- 푸시 전에 원격 변경을 확인한다. 기본 브랜치나 다른 PC에서 이미 공유한 커밋에 강제 푸시, 임의 amend, `reset --hard`를 하지 않는다. 푸시가 거절되면 원격 변경을 읽고 통합한 뒤 다시 검증한다.
- 독립 작업을 여러 PC에서 동시에 진행할 때는 작업별 브랜치를 사용한다. 다른 PC로 옮길 때 미푸시 커밋은 자동으로 전달되지 않음을 유의한다.
- 기능 범위·조작 규칙은 PRD, 구조 결정은 ARCHITECTURE, 다음 할 일은 ROADMAP, 공통 개발 방법은 README에 기록한다. 일회성 로그를 이 파일에 계속 쌓지 않는다.
- 작업 종료 시 현재 브랜치·커밋, 푸시 여부, 검증 결과, 미완료 사항을 간단히 남겨 새 세션에서 이어갈 수 있게 한다.
